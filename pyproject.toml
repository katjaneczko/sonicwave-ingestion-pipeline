[project]
name = "sonicwave-ingestion-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kasia", email = "kat.janeczko@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
sonicwave-ingestion-pipeline = "sonicwave_ingestion_pipeline:main"

[build-system]
requires = ["uv_build>=0.11.23,<0.12.0"]
build-backend = "uv_build"
